<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".FlashcardApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fiszki"
        tools:targetApi="31">
        <activity
            android:name=".ui.createdeck.CreateDeckActivity"
            android:exported="false"
            android:label="@string/title_activity_create_deck"
            android:theme="@style/Theme.Fiszki" />
        <activity
            android:name=".ui.deck.DeckActivity"
            android:exported="false"
            android:label="@string/title_activity_deck"
            android:theme="@style/Theme.Fiszki" />
        <activity
            android:name=".ui.flashcard.FlashcardActivity"
            android:exported="false"
            android:label="@string/title_activity_flashcard"
            android:theme="@style/Theme.Fiszki" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Fiszki">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>